<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="josemarq.musicalapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".nowPlayingActivity"
            android:label="@string/button_now"
            android:parentActivityName=".MainActivity">
        </activity>
        <activity android:name=".myMusicActivity"
            android:label="@string/button_library"
            android:parentActivityName=".MainActivity">
        </activity>
        <activity android:name=".buyMusicActivity"
            android:label="@string/button_buy"
            android:parentActivityName=".MainActivity">
        </activity>
    </application>

</manifest>